"""Experiment run-books (E1..E7). Scripts here are the ONLY place where
truth and nav legitimately meet: truth generates measurements, nav solves,
the script grades and plots. Marked as a package so tests can import the
harness functions."""
